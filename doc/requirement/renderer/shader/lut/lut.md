# renderer/shader/lut 需求

## 1. 目标
本目录保存 shader 查找表数据及其生成脚本。

## 2. 当前使用方
- `renderer/shader/sample.glsl` 会消费这里的 LUT 数据
- 当前查找表文件同时被保留为版本化产物，而不是运行时临时生成

## 3. 功能需求
- 应保留 `.glsl` 形式的查找表输出。
- 若查找表来自生成脚本，应同时保留脚本，保证可再生。
- 修改 `.py` 脚本后，应允许重新生成对应 `.glsl` 数据文件。
