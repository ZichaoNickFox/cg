# geometry 设计

## 1. 角色
`geometry/` 当前是一个很薄的工具模块，核心文件为 `plot.h/.cc`。

## 2. 设计要点
- 目录通过 `cg_geometry` 静态库对外暴露。
- `plot.*` 封装了 ImPlot 的常见调用序列，避免业务场景反复书写 begin/end 和数据桥接代码。
- 函数签名偏向实验友好型 API，而不是抽象复杂的图表系统。

## 3. 边界
- 不在本目录中维护场景对象。
- 不在本目录中管理 GPU 缓冲区或 shader。
