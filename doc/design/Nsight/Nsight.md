# Nsight 设计

## 1. 角色
`Nsight/` 当前只包含 `Nsight.ngfx-proj`，属于外部图形分析工具的辅助元数据目录。

## 2. 设计要点
- 该目录与源码构建链路解耦。
- 文件内容不参与 `CMake`、`ctest` 或运行时逻辑。
- 保留该目录的意义是让性能分析或 GPU 调试配置可以跟随仓库一起演进。

## 3. 边界
- 不在此目录中放置生成产物、trace 导出或一次性本地缓存。
