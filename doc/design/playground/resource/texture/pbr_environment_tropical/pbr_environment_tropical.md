# playground/resource/texture/pbr_environment_tropical 设计

## 1. 角色
该目录是 PBR 环境贴图链路中的环境 cubemap 成果目录。

## 2. 设计要点
- 六面命名遵循常见 cubemap 约定，便于直接装配。
- 这是 `pbr_irradiance_tropical/` 和 `pbr_prefiltered_color_tropical/` 的上游目录。
