#include "renderer/shader/definition.glsl"

/*
struct Shadow {
  mat4 light_space_vp;
  int texture_depth;
};

layout (std430, binding = SSBO_SHADOW_REPO) buffer ShadowRepo {
  Shadow shadow_repo[];
};
*/